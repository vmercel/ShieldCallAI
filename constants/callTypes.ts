/**
 * Shared call-domain value types used by the product path.
 *
 * Previously these lived in constants/mockData.ts alongside dead demo fixtures;
 * the fixtures are gone (P3-4) and the shared types have a home of their own.
 */
export type ThreatLevel = 'safe' | 'warning' | 'danger';
